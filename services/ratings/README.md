# Ratings Service

Python/Flask microservice for managing product ratings using PostgreSQL.

## Setup

```bash
cd services/ratings
python3 -m venv venv
source venv/bin/activate  # On Windows: venv\Scripts\activate
pip install -r requirements.txt